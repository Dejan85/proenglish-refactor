import React from "react";
import { SubContainer, ContentContainer } from "~/src/components/ui";

const Footer = () => {
  return <ContentContainer>footer</ContentContainer>;
};

export default Footer;
