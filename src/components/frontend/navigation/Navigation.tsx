import React from "react";
import { SubContainer, Navigation as Nav } from "../../ui";

const Navigation = (): JSX.Element => {
  return (
    <SubContainer>
      <Navigation />
    </SubContainer>
  );
};

export default Navigation;
